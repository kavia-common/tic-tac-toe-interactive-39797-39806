Assets for Tic Tac Toe (Flutter)

- icons/stark_direwolf.png  -> Marker for Player X (House Stark), tinted with theme.primary
- icons/lannister_lion.png  -> Marker for Player O (House Lannister), tinted with theme.secondary

Both are bundled via pubspec:
flutter:
  assets:
    - assets/
    - assets/icons/

Replace these PNGs with your preferred silhouettes if needed; keep filenames stable to avoid code changes.
