This directory (tic_tac_toe_frontend) is the Flutter project root. It contains:
- pubspec.yaml
- lib/
- android/
- web/
- analysis_options.yaml

CI and tooling should execute `flutter pub get` and analysis from this path.
