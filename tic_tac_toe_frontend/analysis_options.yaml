include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Keep rules minimal; actual project already references flutter_lints.
    # This file exists to aid CI Flutter project root detection.
    avoid_print: false
